export const colors = require('./theme').colors;
export const spacing = require('./theme').spacing;
export const borderRadius = require('./theme').borderRadius;
export const typography = require('./theme').typography;

export { validateEmail, validatePassword, formatPrice, formatPhoneNumber } from './validation';
